spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST}:${DB_PORT:5432}/${DB_NAME}
    username: ${DB_USER}
    password: ${DB_PASS}

app:
  jwt:
    secret: ${JWT_SECRET}

logging:
  level:
    com.restaurant: INFO
    org.springframework.web.socket: WARN
